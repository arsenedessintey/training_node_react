-- CreateTable
CREATE TABLE `Regex` (
    `regex_id` INTEGER NOT NULL AUTO_INCREMENT,
    `choix_regex` VARCHAR(191) NOT NULL,

    PRIMARY KEY (`regex_id`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
